<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- dialog基本样式 -->
    <style name="photo_base_dialog_style" parent="Theme.AppCompat.DayNight.Dialog.Alert">
        <!-- 无边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 浮在 Activity 之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 遮盖层 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 动画效果 -->
        <item name="android:windowEnterAnimation">@anim/photo_dialog_scale_in</item>
        <item name="android:windowExitAnimation">@anim/photo_dialog_scale_out</item>
        <!-- Base.Theme.AppCompat.Light.Dialog.Alert 特有的属性，用于指定默认宽度，这里需要禁用 -->
        <item name="android:windowMinWidthMajor">0dp</item>
        <item name="android:windowMinWidthMinor">0dp</item>
        <!-- 解决 ActionBar 占位导致 Dialog 无法全屏显示的问题 -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- ActionMode覆盖Actionbar，不顶下来 -->
        <item name="windowActionModeOverlay">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- 底部弹出动画 -->
    <style name="photo_dialog_bottom_anim" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/photo_dialog_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/photo_dialog_bottom_out</item>
    </style>

    <!-- Base application theme. -->
    <style name="Photo_AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#007aff</item>
        <item name="colorPrimaryDark">#007aff</item>
        <item name="colorAccent">#007aff</item>

    </style>

</resources>